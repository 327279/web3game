import React from 'react';

const WalletModal: React.FC = () => {
  // This component is obsolete and no longer used.
  // The application now uses the Web3Modal library for all wallet connections,
  // making this custom modal redundant.
  // It has been emptied to prevent build errors and clean up the codebase.
  return null;
};

export default WalletModal;
